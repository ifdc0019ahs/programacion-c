#include <stdio.h>

int main() {
    // Mi primer programa para decir Hello World
    printf("Hola, eres el mejor del Mundo mundial!!"); // Imprime el mensaje en la consola
    // Necesito indicar el tipo de dato que va a devolver la funcion main, tiene que ser un entero, por eso se pone int antes de main
    return 0; // Devuelve 0 para indicar que el programa terminó correctamente
}